<?php


// Sample data for demonstration purposes
$authorId = isset($_GET['id']) ? $_GET['id'] : null;

if ($authorId === null) {
    // Handle invalid author ID
    echo '<p>Invalid author ID.</p>';
} else {
    // Perform the deletion (replace this with your actual database delete operation)
    $isDeleted = true; // Replace with your actual deletion logic

    if ($isDeleted) {
        echo '<p>Author deleted successfully.</p>';
    } else {
        echo '<p>Error deleting author.</p>';
    }
}
?>
